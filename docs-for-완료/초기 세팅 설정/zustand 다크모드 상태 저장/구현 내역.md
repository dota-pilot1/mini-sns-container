# Zustand로 다크모드 상태 저장

## 관련 파일 구조:

```text
mini-sns-front/src
├── shared/stores
│   └── theme-store.ts
├── shared/ui
│   └── theme-toggle.tsx
└── shared/layouts
    └── app-shell.tsx
```

## 구현 체크리스트

- [x] `zustand` 설치
- [x] 테마 store 생성
- [x] `persist` 미들웨어로 저장 처리
- [x] 헤더에 테마 토글 연결
- [x] `data-theme` 기반 테마 반영

## 주요 파일 및 코드 설명:

- [theme-store.ts](/Users/terecal/mini-sns-conainer/mini-sns-front/src/shared/stores/theme-store.ts)
  - `light` / `dark` 상태 저장
  - `toggleTheme` 액션 제공
  - `localStorage`에 `mini-sns-theme` 키로 유지

- [theme-toggle.tsx](/Users/terecal/mini-sns-conainer/mini-sns-front/src/shared/ui/theme-toggle.tsx)
  - 현재 테마 상태를 버튼으로 표시
  - 클릭 시 테마 전환

- [app-shell.tsx](/Users/terecal/mini-sns-conainer/mini-sns-front/src/shared/layouts/app-shell.tsx)
  - `useEffect`로 `document.documentElement.dataset.theme`에 현재 테마 적용
