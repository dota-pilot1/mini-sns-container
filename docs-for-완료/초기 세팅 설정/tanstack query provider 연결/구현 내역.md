# TanStack Query Provider 연결

## 관련 파일 구조:

```text
mini-sns-front/src
└── main.tsx
```

## 구현 체크리스트

- [x] `@tanstack/react-query` 설치
- [x] `QueryClient` 생성
- [x] `QueryClientProvider` 연결
- [x] 전체 앱에서 사용할 수 있게 루트에 적용

## 주요 파일 및 코드 설명:

- [main.tsx](/Users/terecal/mini-sns-conainer/mini-sns-front/src/main.tsx)
  - `const queryClient = new QueryClient()`로 클라이언트 생성
  - `QueryClientProvider`로 전체 앱을 감쌈
  - 이후 서버 상태 관리 로직을 추가할 기반으로 사용
