# react-i18next 기본 연결

## 관련 파일 구조:

```text
mini-sns-front/src
├── shared
│   └── i18n.ts
├── main.tsx
└── routes
    ├── home-page.tsx
    ├── login-page.tsx
    └── signup-page.tsx
```

## 구현 체크리스트

- [x] `i18next` 설치
- [x] `react-i18next` 설치
- [x] i18n 초기화 파일 추가
- [x] 앱 진입점에서 i18n 로드
- [x] 각 화면에서 `useTranslation` 적용

## 주요 파일 및 코드 설명:

- [i18n.ts](/Users/terecal/mini-sns-conainer/mini-sns-front/src/shared/i18n.ts)
  - 언어 리소스 정의
  - `initReactI18next`로 React에 연결
  - 기본 언어 `ko`, fallback 언어 `en`으로 설정

- [main.tsx](/Users/terecal/mini-sns-conainer/mini-sns-front/src/main.tsx)
  - `@/shared/i18n` import로 앱 시작 시 초기화

- 각 라우트 페이지
  - `useTranslation()`의 `t()`로 문구를 출력
