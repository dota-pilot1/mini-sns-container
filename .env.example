# Copy to .env and fill real values. .env is gitignored.

# --- Database ---
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/stayops
SPRING_DATASOURCE_USERNAME=stayops
SPRING_DATASOURCE_PASSWORD=stayops1234

# --- Server ---
SERVER_PORT=8080

# --- JWT ---
# Must be >= 32 bytes for HS256. Generate with: openssl rand -base64 48
JWT_SECRET=
